{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="container col-sm-4 mt-5 d-flex justify-content-center align-items-center">
        <div class="card-body text-white rounded-pill border border-dark">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mt-2 mb-2">
                    Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a class="" href="{{ path('app_logout') }}">Me déconnecter</a>
                </div>
            {% endif %}
        </div>
    </div>

    <div class="container col-sm-4 d-flex justify-content-center align-items-center" style="height: 50vh;">
        <div class="card-body rounded-7 mt-2 mb-2 border border-dark d-flex justify-content-center">
            <div class="form-group" style="padding: 20px">
                <h3>Vous n'avez pas encore de compte ? <br> <a style="text-decoration-line: underline; font-weight: bold;" class="" href="{{ path('app_register') }}">Inscrivez-Vous</a> </h3>
                <form method="post" class="center-card">

                    <input type="email" value="{{ last_username }}" name="email" placeholder="Email" id="inputEmail" style="background: #333536; border-radius: 15px;" class="form-control place-color" autocomplete="email" required autofocus>
                    <input type="password" name="password" id="inputPassword" placeholder="Mot de passe" style="background: #333536; border-radius: 15px;" class="form-control place-color mt-3" autocomplete="current-password" required>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    <a href="{{ path('forgotten_password') }}">Mot de passe oublié ?</a>
                    <button type="submit" class="button">Connexion</button>
                </form>
            </div>
        </div>
    </div>

{% endblock %}
